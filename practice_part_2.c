#include<stdio.h>
int main(){
    int height = 7;
    int width = 5;
    int perimeter = 2*(height + width);
    printf("perimeter of Revtangle is %d\n ", perimeter);
    int area = height*width;
    printf("area of Rectangle is %d", area);
    return 0; 
}